using UnityEngine;
using System.Collections;

/**
 * This enum represents the type to be used in EntityBehaviours (still not used)
 */ 
public enum Type {
	None,
	PlayerController,
	Player,
	Enemy,
	HP,
	MP,
	SP
}
